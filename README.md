## Preparation
Install virtual environment for Python packages
```pyhton
python -m venv virt_env
source virt_env/bin/activate
```

And install the following packages:

```bash
pip install iodd-parser
pip install python-dotenv
```